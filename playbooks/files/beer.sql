USE beer;
CREATE TABLE IF NOT EXISTS types(name varchar(10), rating varchar(10));
INSERT INTO types(name,rating)  VALUES("stout","good");
INSERT INTO types(name,rating)  VALUES("porter","better");
INSERT INTO types(name,rating)  VALUES("free","best");
